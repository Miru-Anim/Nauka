<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <p id="wynik"></p>
    <script>
        var wypisz=document.getElementById("wynik");
        wypisz.innerHTML="ZAD 1 Instrukcja warunkowa<br>";

        var a=2;
        var b=3;
        var x=a/b;

        if(b==0)
        {
            wypisz.innerHTML+="nie można dzielić przez zero ";
        }
        else    
        {
            wypisz.innerHTML+="można podielić i tyle wynosi x działaniw "+x;
        }



        wypisz.innerHTML+="<br>ZAD2<br>";

        var a=2;
        var b=3;
        var c=5;
        var d=1;
        var y= a/b+c/d;

        if(b==0 || d==0)
        {
            wypsiz.innerHTML+="nie można dzielić przez zero<br>";
        }
        else 
        {
            wypisz.innerHTML+="można podzielić i dodać gdzie y będzei równe"+y+"<br>";
        }
        


        wypisz.innerHTML+="<br>ZAD5<br>";

        var d=5;
        var g=5;
        var h=d%g;

        if(h==0)
        {
         wypisz.innerHTML+="liczba jest podzielna bez reszty<br>";
        }
        else if (h>0)
        {
         wypisz.innerHTML+="liczba jest podzielna lecz z resztą<br>";
        }
        else
        {
         wypisz.innerHTML+="liczba nie podzielna";
        }


        wypisz.innerHTML+="<br>ZAD6<br>";
        
        var g=-6;

        if(g>0)
        {
            wypisz.innerHTML+="liczba dodatnia";
        }
        else if(g=0)
        {
            wypisz.innerHTML+="liczba równa zero";
        }
        else
        {
            wypisz.innerHTML+="liczba mniejsza od zero";
        }


        
    </script>
</body>
</html>